# fhir-format landing page

Vite + React one-page site used for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The repository workflow builds this app from `docs/page` and deploys `docs/page/dist` to GitHub Pages.
